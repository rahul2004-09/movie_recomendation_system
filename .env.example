# Copy this file to .env and fill in your own values. Never commit .env.

# TMDB API key (v3 auth) - get free at https://www.themoviedb.org/settings/api
# Optional: if unset or unreachable, posters fall back to Wikipedia automatically.
TMDB_API_KEY=
